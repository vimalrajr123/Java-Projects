package com.jp.interview;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

public class SampleCode {
	
	
	private static void swap(int a, int b) {
	
		
		/*Comparator<Employee> cmp = Comparator.comparing(Employee:: name);
		Collections.sort
		int sal 
		
		a, mep f l id sal
		b
		c*/
		
		/*
		 * List <Employee> l = new ArrayList<>(); l.stream(). .sorted(e ->
		 * Comparator.comparing(Employee:: lastName)) .forEach(s ->
		 * System.out.println(s.lastName + " " + s.salary);
		 * 
		 * 
		 */
		
		
		
		
		
		
		
	}
	 
}
